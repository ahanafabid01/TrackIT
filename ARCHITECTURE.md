# TrackIt System Architecture

## 🏗️ Multi-Tenant Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         DATABASE: trackit                        │
│                                                                   │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │                      users TABLE                            │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  Owner 1 (owner_id: NULL)                            │  │ │
│  │  │    ├── Moderator 1 (owner_id: Owner1_ID)            │  │ │
│  │  │    ├── Accountant 1 (owner_id: Owner1_ID)           │  │ │
│  │  │    ├── Admin In-charge 1 (owner_id: Owner1_ID)      │  │ │
│  │  │    └── Store In-charge 1 (owner_id: Owner1_ID)      │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  │                                                              │ │
│  │  ┌──────────────────────────────────────────────────────┐  │ │
│  │  │  Owner 2 (owner_id: NULL)                            │  │ │
│  │  │    ├── Moderator 2 (owner_id: Owner2_ID)            │  │ │
│  │  │    ├── Accountant 2 (owner_id: Owner2_ID)           │  │ │
│  │  │    ├── Admin In-charge 2 (owner_id: Owner2_ID)      │  │ │
│  │  │    └── Store In-charge 2 (owner_id: Owner2_ID)      │  │ │
│  │  └──────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘

🔒 ISOLATION: Owner 1's team CANNOT access Owner 2's data
```

---

## 🔄 User Flow Diagram

```
┌──────────────────────────────────────────────────────────────────┐
│                     auth/auth.php (Landing)                       │
└────────────────┬──────────────────────────┬──────────────────────┘
                 │                          │
         ┌───────▼─────────┐        ┌──────▼────────┐
         │   SIGNUP        │        │    LOGIN      │
         │  (Owner Only)   │        │  (All Roles)  │
         └───────┬─────────┘        └──────┬────────┘
                 │                          │
                 │                    ┌─────▼──────────────────────┐
                 │                    │  Role-Based Redirection    │
                 │                    └─────┬──────────────────────┘
                 │                          │
                 └──────────────────────────┼─────────────┐
                                           │             │
        ┌──────────────────┬───────────────┼─────┬───────┼─────────┐
        │                  │               │     │       │         │
┌───────▼────────┐  ┌──────▼──────┐  ┌────▼──┐ ┌▼────┐ ┌▼──────┐  │
│ Owner          │  │ Moderator   │  │Acct.  │ │Admin│ │Store  │  │
│ Dashboard      │  │ Dashboard   │  │Dash.  │ │Dash.│ │Dash.  │  │
│                │  │             │  │       │ │     │ │       │  │
│ - Manage Team  │  │ - Bookings  │  │- Pay  │ │-GRN │ │-Deliv │  │
│ - Analytics    │  │ - Customers │  │- Inv  │ │-Inv │ │-Scan  │  │
│ - Reports      │  │ - Products  │  │- Rep  │ │-Sup │ │-Ret   │  │
└────────────────┘  └─────────────┘  └───────┘ └─────┘ └───────┘  │
```

---

## 🔐 Security Layers

```
┌────────────────────────────────────────────────────────────┐
│ Layer 1: Authentication (login.php)                        │
│   ├── Email & Password Verification                        │
│   ├── Session Creation                                     │
│   └── Status Check (Active/Inactive/Suspended)             │
└────────────────────────────────────────────────────────────┘
                           │
┌────────────────────────────────────────────────────────────┐
│ Layer 2: Authorization (requireRole() in config.php)      │
│   ├── Role Verification                                    │
│   ├── Page Access Control                                  │
│   └── Redirect if Unauthorized                             │
└────────────────────────────────────────────────────────────┘
                           │
┌────────────────────────────────────────────────────────────┐
│ Layer 3: Data Isolation (owner_id filtering)              │
│   ├── SQL Query Filtering by owner_id                      │
│   ├── Only Show Owner's Team Data                          │
│   └── Prevent Cross-Owner Data Access                      │
└────────────────────────────────────────────────────────────┘
```

---

## 📊 Database Relationships

```
┌──────────────────────────────────────────────────┐
│               users TABLE                         │
│                                                   │
│  id (PK) ───────────┐                            │
│  name               │                             │
│  email (UNIQUE)     │                             │
│  password           │                             │
│  role (ENUM)        │                             │
│  owner_id (FK) ─────┼──► SELF REFERENCE          │
│  status (ENUM)      │    (References users.id)    │
│  created_at         │                             │
│  last_login         │                             │
│                     │                             │
│  Indexes:           │                             │
│    - idx_owner_id   │                             │
│    - idx_email      │                             │
│    - idx_role       │                             │
└─────────────────────┴─────────────────────────────┘

Constraint: fk_owner
  - ON DELETE CASCADE
  - Ensures when Owner is deleted, all their team members are deleted
```

---

## 🎯 Role Permissions Matrix

```
┌──────────────────┬───────┬──────────┬───────────┬──────────┬─────────┐
│ Feature          │ Owner │Moderator │Accountant │Admin I-C │Store I-C│
├──────────────────┼───────┼──────────┼───────────┼──────────┼─────────┤
│ Manage Users     │   ✓   │    ✗     │     ✗     │    ✗     │    ✗    │
│ View Dashboard   │   ✓   │    ✓     │     ✓     │    ✓     │    ✓    │
│ Bookings         │   ✓   │    ✓     │     ✗     │    ✗     │    ✓    │
│ Customers        │   ✓   │    ✓     │     ✗     │    ✗     │    ✗    │
│ Payments         │   ✓   │    ✗     │     ✓     │    ✗     │    ✗    │
│ Invoices         │   ✓   │    ✗     │     ✓     │    ✗     │    ✗    │
│ Inventory        │   ✓   │    ✗     │     ✗     │    ✓     │    ✓    │
│ GRN              │   ✓   │    ✗     │     ✗     │    ✓     │    ✗    │
│ Suppliers        │   ✓   │    ✗     │     ✗     │    ✓     │    ✗    │
│ Delivery         │   ✓   │    ✗     │     ✗     │    ✗     │    ✓    │
│ Barcode Scan     │   ✓   │    ✗     │     ✗     │    ✓     │    ✓    │
│ Returns          │   ✓   │    ✗     │     ✗     │    ✓     │    ✓    │
│ Reports          │   ✓   │    ✓     │     ✓     │    ✓     │    ✓    │
└──────────────────┴───────┴──────────┴───────────┴──────────┴─────────┘
```

---

## 📂 File Organization

```
trackit/
│
├── auth/                          # Authentication Module
│   ├── auth.php                   # Login/Signup UI
│   ├── auth.js                    # Frontend Logic
│   ├── login.php                  # Login Handler
│   ├── signup.php                 # Signup Handler (Owner Only)
│   └── css/
│       └── auth.css               # Authentication Styles
│
├── config/
│   └── config.php                 # DB Config + Helper Functions
│                                  # - requireAuth()
│                                  # - requireRole()
│                                  # - getOwnerId()
│                                  # - redirectToDashboard()
│
├── main/
│   ├── owner_dashboard.php        # Owner Dashboard
│   ├── add_user.php               # Create Team Member
│   ├── delete_user.php            # Delete Team Member
│   ├── logout.php                 # Logout Handler
│   │
│   ├── css/
│   │   ├── styles.css             # Common Dashboard Styles
│   │   └── dashboard-common.js    # Common JS Functions
│   │
│   └── pages/                     # Role-Based Dashboards
│       ├── moderator.php
│       ├── accountant.php
│       ├── admin_in-charge.php
│       ├── store_in-charge.php
│       │
│       ├── css/                   # Role-Specific Styles
│       │   ├── moderator.css
│       │   ├── accountant.css
│       │   ├── admin_in-charge.css
│       │   └── store_in-charge.css
│       │
│       └── js/                    # Role-Specific Scripts
│           ├── moderator.js
│           ├── accountant.js
│           ├── admin_in-charge.js
│           └── store_in-charge.js
│
└── sql/
    └── users.sql                  # Database Schema
```

---

## 🔄 Data Flow Example: Owner Creates Team Member

```
1. Owner Dashboard (owner_dashboard.php)
   └─► Click "Add New User"
        └─► Modal Opens with Form

2. User Fills Form
   ├─► Name: "John Doe"
   ├─► Email: "john@example.com"
   ├─► Role: "Moderator"
   └─► Password: "********"

3. JavaScript (owner_dashboard.php <script>)
   └─► FormData sent to add_user.php via AJAX

4. Backend (add_user.php)
   ├─► Verify Owner is logged in
   ├─► Get owner_id from session ($_SESSION['user_id'])
   ├─► Hash password
   ├─► Insert into database:
   │     INSERT INTO users (name, email, password, role, owner_id)
   │     VALUES ('John Doe', 'john@...', '$2y$...', 'Moderator', 1)
   └─► Return JSON response

5. Frontend Response
   ├─► If success: Show success message, reload page
   └─► If error: Show error message

6. New User Login (login.php)
   ├─► Enter credentials
   ├─► Verify password
   ├─► Set session variables
   ├─► Redirect based on role (Moderator → moderator.php)
   └─► Moderator Dashboard Loads
```

---

## 🎨 UI Component Structure

```
┌────────────────────────────────────────────────────────────┐
│                       DASHBOARD                             │
│  ┌──────────┐  ┌──────────────────────────────────────┐   │
│  │          │  │         HEADER                        │   │
│  │          │  │  ┌──────────┐  ┌─────────────────┐   │   │
│  │ SIDEBAR  │  │  │ Search   │  │ Notifications   │   │   │
│  │          │  │  └──────────┘  │ User Profile    │   │   │
│  │ - Logo   │  │                └─────────────────┘   │   │
│  │ - Nav    │  └──────────────────────────────────────┘   │
│  │ - Items  │                                              │
│  │ - Logout │  ┌──────────────────────────────────────┐   │
│  │          │  │      MAIN CONTENT AREA                │   │
│  └──────────┘  │                                       │   │
│                │  - Stats Cards                        │   │
│                │  - Data Tables                        │   │
│                │  - Charts                             │   │
│                │  - Forms                              │   │
│                │                                       │   │
│                └──────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

---

**This architecture ensures**:
- ✅ Complete data isolation between owners
- ✅ Role-based access control
- ✅ Scalable multi-tenant design
- ✅ Secure authentication and authorization
- ✅ Clean, maintainable code structure
